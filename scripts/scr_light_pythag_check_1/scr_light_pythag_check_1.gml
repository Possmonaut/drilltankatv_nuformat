function scr_light_pythag_check_1() {
	//argument0 = which case to use aka which wall we hit 
	//get angle from two corner point intersections and radial FROM two corner point intersections
	//get side side angle using new angle and the sides calculated from the above info 
	//basically using the radial angle of the missing XY, as it intersects the old value 
	//basically, get triangle data, then change one angle and calculate the new difference.
	//so angle b, new angle a, get angle c using the 360-a+b
	//then get x+y by plugging into SSA or whatever




}
