/*

var gui_width = display_get_gui_width();
var gui_height = display_get_gui_height();

var XX = ((x - obj_ow_camera.x) * (gui_width / 1024))+gui_width/2;
var YY = ((y - obj_ow_camera.y) * (gui_height / 768))+gui_height/2;

haze_rect_pos(activeHaze, XX, YY);
*/

//remove this if you fix the issue in the create event 
 //later set this up to autotile 