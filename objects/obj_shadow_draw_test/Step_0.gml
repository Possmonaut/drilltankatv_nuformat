/// @description Insert description here
// You can write your code in this editor

/*
var listLeng = ds_list_size(activeList);

for( var i = 0; i < listLeng; i ++)
{
	
	if(activeList[|i] = undefined)
	{
		ds_list_delete(activeList,i);
		//i --;
	}
	else
	{
		if(!instance_exists(activeList[|i]))
		{
			ds_list_delete(activeList,i);
			//i --;
		}
	}
}