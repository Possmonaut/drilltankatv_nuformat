velo = 1;
veloDecay = -0.005;
depth = 1;

image_num = sprite_get_number(sp_salp_2);

dir = irandom(359);
//note. for the tentacles, later, you would need to add or lose angles, 
//but also fed throug hthe angle difference function. which is a nightmare. test it later