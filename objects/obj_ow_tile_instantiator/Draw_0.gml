//draw_self();

//draw_text(target.x, target.y - 32, update); 

/*
var inst_pos = 0;

inst_pos = instance_position(mouse_x,mouse_y,obj_ow_nopass);

if(inst_pos != -4)
{
draw_text(mouse_x,mouse_y,inst_pos.object_index);
}